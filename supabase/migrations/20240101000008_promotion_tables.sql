-- Superseded by 20240101000007_promotion_schedule.sql, which already creates
-- promotion_log and student_promotion_history. Re-running the same
-- CREATE POLICY statements here failed on a fresh database, so this file is
-- intentionally a no-op kept only to preserve migration ordering.
SELECT 1;
