# dependencies
node_modules/
.pnp
.pnp.js

# production
.next/
out/

# env files — NEVER commit these
.env
.env.local
.env*.bak
.env.development.local
.env.test.local
.env.production.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
.vercel

# Build artifacts — never commit these
tsconfig.tsbuildinfo
*.tsbuildinfo

# Accidental archives committed to source
src/lib.zip
src/**/*.zip
src/app/**/*.zip

# Local data backups (contain personal data)
scripts/backups/

# Student lists (personal data) — keep local
scripts/data/

# Local tool settings
.claude/
